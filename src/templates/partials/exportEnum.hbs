{{#if description}}
/**
 * {{{description}}}
 */
{{/if}}
export type {{{name}}} =
{{#each enum}}
    {{#if description}}
        /**
        * {{{description}}}
        */
    {{/if}}
        | {{{value}}}
{{/each}}
