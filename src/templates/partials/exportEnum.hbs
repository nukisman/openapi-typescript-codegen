{{#if description}}
/**
 * {{{description}}}
 */
{{/if}}
export const {{{camelcase name}}}Values = [
{{#each enum}}
    {{#if description}}
        /**
        * {{{description}}}
        */
    {{/if}}
    {{{value}}},
{{/each}}
] as const;

export type {{{name}}} = typeof {{{camelcase name}}}Values[number];
